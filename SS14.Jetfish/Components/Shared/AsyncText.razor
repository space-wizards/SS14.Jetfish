@_text

@code {

    private string _text = string.Empty;
    
    [Parameter]
    public Func<Task<string>>? Text { get; set; }
    
    protected override async Task OnParametersSetAsync()
    {
        if (Text == null)
            return;
        
        _text = await Text.Invoke();
    }
}